import React from 'react';

const PageTitle = ({ children }) => {
  return <div className="text-4xl tracking-wide text-pageTitle font-bold font-title justify-start">{children}</div>;
};

export default PageTitle;
